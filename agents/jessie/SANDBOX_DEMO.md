# Sandbox demo

Set `JESSE_DATA_PATH` to a temporary ignored file, then run:

```bash
python -m agents.jessie.cli run-demo
```

The demo creates fake local intake data and simulates Calendar, Sheets, Gmail, and n8n results. It performs no network activity and prints redacted output only. Delete the temporary data after the demonstration.
