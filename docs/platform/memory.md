# Memory engine

Short-term, long-term, and conversation memories persist in SQLite. Text search is operational. Conversation summaries use a deterministic staging implementation. A vector field and semantic-search interface are present, but vector embedding and similarity search remain explicitly unavailable placeholders.
