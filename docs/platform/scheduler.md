# Scheduling

The local calendar abstraction supports appointments, availability checks, rescheduling, and overlap detection. It makes no Google Calendar or other network calls. All datetimes should be timezone-aware. External synchronization and distributed conflict locking are production work.
