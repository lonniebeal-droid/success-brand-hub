# Dashboards

`/executive-dashboard` presents staging operational categories and agent readiness. `/admin` presents administration areas for users, roles, permissions, logs, queues, memory, settings, API keys, and feature flags. These pages contain no credentials or production controls; authenticated server-side data wiring remains a staging deployment step.
